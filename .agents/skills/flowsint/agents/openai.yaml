interface:
  display_name: "Flowsint"
  short_description: "Repo-specific patterns and workflows for flowsint"
  default_prompt: "Use the flowsint repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true